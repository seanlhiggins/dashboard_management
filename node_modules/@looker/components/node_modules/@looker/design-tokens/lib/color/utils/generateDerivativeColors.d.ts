import { DerivativeColors, SpecifiableColors } from '../types';
export declare const generateDerivativeColors: ({ background, inform, link, positive, text, warn, }: SpecifiableColors) => DerivativeColors;
