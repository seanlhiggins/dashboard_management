import React, { ReactElement, RefAttributes, Ref } from 'react';
import { ButtonProps, IconButtonProps } from '.';
export interface MultiFunctionButtonProps {
    alternate: ReactElement<(ButtonProps | IconButtonProps) & RefAttributes<HTMLButtonElement>>;
    alternateRef?: Ref<HTMLButtonElement>;
    children: ReactElement<(ButtonProps | IconButtonProps) & RefAttributes<HTMLButtonElement>>;
    swap?: boolean;
}
export declare const MultiFunctionButton: React.ForwardRefExoticComponent<MultiFunctionButtonProps & React.RefAttributes<HTMLButtonElement>>;
export interface MultiFunctionButtonLayoutProp {
    swap?: boolean;
    height: number;
    width: number;
}
