import { CompatibleHTMLProps, StatefulColor } from '@looker/design-tokens';
import { MaxWidthProps, MinWidthProps, SpaceProps, WidthProps } from 'styled-system';
import { IconType } from '../Icon';
import { ButtonSizeProps, ButtonSizes } from './size';
export interface ButtonIconProps {
    iconBefore?: IconType;
    iconAfter?: IconType;
}
export interface ButtonBaseProps extends Omit<CompatibleHTMLProps<HTMLButtonElement>, 'type'>, ButtonSizeProps, MaxWidthProps, MinWidthProps, WidthProps, SpaceProps {
    type?: 'button' | 'submit' | 'reset';
    color?: StatefulColor;
    focusVisible?: boolean;
}
export interface ButtonProps extends ButtonBaseProps, ButtonIconProps {
    size?: ButtonSizes;
    fullWidth?: boolean;
}
