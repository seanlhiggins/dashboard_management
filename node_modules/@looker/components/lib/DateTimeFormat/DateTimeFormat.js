import "core-js/modules/es.date.to-string.js";
import "core-js/modules/es.date.now.js";
import "core-js/modules/es.array.concat.js";
import React from 'react';
import repeat from 'lodash/repeat';
import trim from 'lodash/trim';
import { Locales, formatDateString } from '../utils/i18n';
export var DateTimeFormat = function DateTimeFormat(_ref) {
  var _ref$children = _ref.children,
      children = _ref$children === void 0 ? new Date(Date.now()) : _ref$children,
      _ref$date = _ref.date,
      date = _ref$date === void 0 ? true : _ref$date,
      _ref$format = _ref.format,
      format = _ref$format === void 0 ? 'medium' : _ref$format,
      _ref$locale = _ref.locale,
      locale = _ref$locale === void 0 ? Locales.English : _ref$locale,
      _ref$time = _ref.time,
      time = _ref$time === void 0 ? true : _ref$time,
      timeZone = _ref.timeZone;
  var repetitions = {
    full: 4,
    "long": 3,
    medium: 2,
    "short": 1
  };
  var dateFormat = repeat('P', repetitions[format]);
  var timeFormat = repeat('p', repetitions[format]);
  var timeZoneFormat = repeat('z', repetitions[format]);
  var combinedDateTimeFormat = trim("".concat(date ? dateFormat : '').concat(time ? timeFormat : '', " ").concat(timeZone ? timeZoneFormat : ''));

  try {
    return React.createElement(React.Fragment, null, formatDateString(children, locale, combinedDateTimeFormat, timeZone));
  } catch (error) {
    return React.createElement(React.Fragment, null, error);
  }
};
//# sourceMappingURL=DateTimeFormat.js.map