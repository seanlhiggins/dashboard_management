import React from 'react';
import { IconType } from '../Icon';
import { AvatarProps } from './Avatar';
export interface AvatarIconProps extends AvatarProps {
    icon?: IconType;
    color?: string;
    bg?: string;
}
export declare const AvatarIcon: import("styled-components").StyledComponent<React.FC<AvatarIconProps>, import("styled-components").DefaultTheme, {
    bg: string;
    color: string;
    size: string;
}, "color" | "size" | "bg">;
