import { itemSelectedColor } from '@looker/design-tokens';
import { css } from 'styled-components';
export var listItemBackgroundColor = function listItemBackgroundColor(_ref) {
  var keyColor = _ref.keyColor,
      current = _ref.current,
      disabled = _ref.disabled,
      hovered = _ref.hovered,
      selected = _ref.selected,
      colors = _ref.theme.colors;
  var keyColors = {
    all: colors.keySubtle,
    hovered: colors.ui1,
    selected: colors.keySubtle
  };
  var uiColors = {
    all: itemSelectedColor(colors.ui2),
    hovered: colors.ui1,
    selected: itemSelectedColor(colors.ui2)
  };
  var stateColors = keyColor ? keyColors : uiColors;
  var renderedColor;
  if (disabled) renderedColor = 'transparent';else if ((selected || current) && hovered) renderedColor = stateColors.all;else if (selected || current) renderedColor = stateColors.selected;else if (hovered) renderedColor = stateColors.hovered;else renderedColor = 'transparent';
  return css(["background:", ";"], renderedColor);
};
//# sourceMappingURL=listItemBackgroundColor.js.map