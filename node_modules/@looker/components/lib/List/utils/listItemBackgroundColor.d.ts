import { Theme } from '@looker/design-tokens';
import { ListItemStatefulWithHoveredProps } from '../types';
export declare const listItemBackgroundColor: ({ keyColor, current, disabled, hovered, selected, theme: { colors }, }: ListItemStatefulWithHoveredProps & {
    theme: Theme;
}) => import("styled-components").FlattenSimpleInterpolation;
