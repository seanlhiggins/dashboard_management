import { SpaceRamp } from '@looker/design-tokens';
import { ListItemDimensions } from '../types';
export declare const listItemPadding: ({ px: propsPx, py: propsPy, theme: { space }, }: {
    theme: {
        space: SpaceRamp;
    };
} & Pick<ListItemDimensions, "px" | "py">) => import("styled-components").FlattenSimpleInterpolation;
