import { CompatibleHTMLProps } from '@looker/design-tokens';
import React, { ReactNode } from 'react';
import { IconType } from '../Icon';
import { DensityRamp, Detail, ListItemRole, ListItemStatefulProps } from './types';
export interface ListItemProps extends CompatibleHTMLProps<HTMLElement>, ListItemStatefulProps {
    color?: string;
    density?: DensityRamp;
    description?: ReactNode;
    detail?: Detail;
    icon?: IconType;
    itemRole?: ListItemRole;
    truncate?: boolean;
}
export declare const ListItem: import("styled-components").StyledComponent<React.ForwardRefExoticComponent<ListItemProps & React.RefAttributes<HTMLLIElement>>, import("styled-components").DefaultTheme, {}, never>;
