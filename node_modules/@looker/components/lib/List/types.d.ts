import { FontSizes, LineHeights, SpacingSizes } from '@looker/design-tokens';
import { ReactNode } from 'react';
import { IconSize } from '..';
export declare type DensityRamp = -3 | -2 | -1 | 0 | 1;
export interface ListItemDimensions {
    descriptionFontSize: FontSizes;
    descriptionLineHeight: LineHeights;
    height: number;
    px: SpacingSizes;
    py: SpacingSizes | '0.375rem';
    iconGap: SpacingSizes;
    iconSize: IconSize;
    labelFontSize: FontSizes;
    labelLineHeight: LineHeights;
}
export declare const listItemDimensionKeys: string[];
export interface ListItemStatefulProps {
    keyColor?: boolean;
    disabled?: boolean;
    current?: boolean;
    selected?: boolean;
}
export interface ListItemStatefulWithHoveredProps extends ListItemStatefulProps {
    hovered?: boolean;
}
interface DetailOptions {
    accessory?: boolean;
    hoverDisclosure?: boolean;
}
export declare type Detail = ReactNode | {
    content: ReactNode;
    options: DetailOptions;
};
export declare type ListItemRole = 'button' | 'link' | 'none';
export {};
