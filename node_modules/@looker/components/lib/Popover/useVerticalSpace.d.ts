import { CSSProperties } from 'react';
import { Placement } from '@popperjs/core';
export declare const useVerticalSpace: (element: HTMLElement | null, pin: boolean, placement: Placement, isOpen: boolean, popperStyle: CSSProperties) => number;
