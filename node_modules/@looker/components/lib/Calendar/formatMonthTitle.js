import "core-js/modules/es.array.concat.js";
import { LocaleUtils } from 'react-day-picker';
export var formatMonthTitle = function formatMonthTitle(localization) {
  return function (month) {
    if (localization !== null && localization !== void 0 && localization.months) {
      return "".concat(localization.months[month.getMonth()], " ").concat(month.getFullYear());
    } else {
      return LocaleUtils.formatMonthTitle(month);
    }
  };
};
//# sourceMappingURL=formatMonthTitle.js.map