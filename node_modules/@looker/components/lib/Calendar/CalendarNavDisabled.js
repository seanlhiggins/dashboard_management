import React from 'react';
import { Heading } from '../Text';
import { headingSizeMap } from './CalendarNav';
export var CalendarNavDisabled = function CalendarNavDisabled(_ref) {
  var localeUtils = _ref.localeUtils,
      month = _ref.month,
      size = _ref.size;
  return React.createElement(Heading, {
    as: headingSizeMap(size),
    fontWeight: "semiBold",
    fontFamily: "body",
    textAlign: "center",
    py: "xsmall"
  }, localeUtils.formatMonthTitle(month));
};
CalendarNavDisabled.displayName = 'CalendarNavDisabled';
//# sourceMappingURL=CalendarNavDisabled.js.map