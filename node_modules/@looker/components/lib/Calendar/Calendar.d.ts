import React from 'react';
import { RangeModifier } from 'react-day-picker';
import { CalendarLocalization } from './types';
interface CalendarProps {
    className?: string;
    disabled?: boolean;
    localization?: CalendarLocalization;
    onDayClick?: (day: Date) => void;
    onMonthChange?: (month: Date) => void;
    onNextClick?: (date: Date) => void;
    onNowClick?: (date: Date) => void;
    onPrevClick?: (date: Date) => void;
    readOnly?: boolean;
    selectedDates?: Date | Date[] | RangeModifier;
    showNextButton?: boolean;
    showPreviousButton?: boolean;
    viewMonth?: Date;
}
export declare const Calendar: import("styled-components").StyledComponent<React.FC<CalendarProps>, import("styled-components").DefaultTheme, {
    size: string;
}, "size">;
export {};
