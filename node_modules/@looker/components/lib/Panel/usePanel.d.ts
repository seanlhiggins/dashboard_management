import { UsePanelProps, UsePanelResponse } from './types';
export declare const usePanel: ({ canClose, content, defaultOpen, direction, isOpen: controlledIsOpen, onClose, setOpen: controlledSetOpen, title, }: UsePanelProps) => UsePanelResponse;
