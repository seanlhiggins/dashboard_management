import React, { MouseEvent } from 'react';
import { UseTooltipProps } from './types';
export declare function useTooltip({ canClose, content, isOpen: initializeOpen, width, maxWidth, textAlign, disabled, id, invert, triggerElement, placement: propsPlacement, delay, }: UseTooltipProps): {
    domProps: {
        'aria-describedby': string;
        className: string;
        onBlur: () => void;
        onFocus: () => void;
        onMouseOut: (event: MouseEvent<HTMLElement>) => void;
        onMouseOver: () => void;
        ref: (node: HTMLElement | null) => void;
    };
    popperInstanceRef: React.MutableRefObject<import("@popperjs/core").Instance | undefined>;
    tooltip: JSX.Element | null;
};
