import "core-js/modules/es.array.concat.js";
import { css } from 'styled-components';
import { indicatorDefaults } from './indicatorDefaults';
export var generateIndent = function generateIndent(depth, indicatorSize, theme) {
  var sizes = theme.sizes,
      space = theme.space;
  var indicatorGap = indicatorDefaults.indicatorGap;
  var treePaddingSize = space.xxsmall;
  var indicatorIconSize = sizes[indicatorSize];
  var indicatorGapSize = space[indicatorGap];
  var indentCalculation = "".concat(treePaddingSize, " + (").concat(indicatorIconSize, " + ").concat(indicatorGapSize, ") * ").concat(depth);
  return css(["padding-left:calc(", ");"], indentCalculation);
};
//# sourceMappingURL=generateIndent.js.map