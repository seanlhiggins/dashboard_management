import { ReactNode } from 'react';
import { AccordionProps } from '../Accordion';
import { ListItemProps } from '../List';
export interface TreeProps extends Omit<AccordionProps, 'children' | 'indicatorGap' | 'indicatorPosition' | 'indicatorSize'>, ListItemProps {
    border?: boolean;
    branchFontWeight?: boolean;
    dividers?: boolean;
    label: ReactNode;
}
