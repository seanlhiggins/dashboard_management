import React from 'react';
import { List } from '../List';
import { listItemDimensions } from '../List/utils';
import { TreeContext } from './TreeContext';
import { TreeStyle } from './TreeStyle';
export var TreeArtificial = function TreeArtificial(_ref) {
  var children = _ref.children,
      _ref$density = _ref.density,
      density = _ref$density === void 0 ? 0 : _ref$density;

  var _listItemDimensions = listItemDimensions(density),
      iconSize = _listItemDimensions.iconSize;

  return React.createElement(TreeStyle, {
    depth: -1,
    indicatorSize: iconSize,
    dividers: true
  }, React.createElement(TreeContext.Provider, {
    value: {
      density: density,
      depth: 0
    }
  }, React.createElement(List, null, children)));
};
//# sourceMappingURL=TreeArtificial.js.map