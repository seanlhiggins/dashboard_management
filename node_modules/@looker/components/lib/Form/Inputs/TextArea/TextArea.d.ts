import React from 'react';
import { CompatibleHTMLProps } from '@looker/design-tokens';
import { ValidationType } from '../../ValidationMessage';
import { SimpleLayoutProps } from '../../../Layout/utils/simple';
declare type TextAreaResize = 'vertical' | 'none' | boolean;
export interface TextAreaProps extends Omit<SimpleLayoutProps, 'size'>, CompatibleHTMLProps<HTMLTextAreaElement> {
    resize?: TextAreaResize;
    validationType?: ValidationType;
}
export declare const TextArea: import("styled-components").StyledComponent<React.FC<TextAreaProps>, import("styled-components").DefaultTheme, TextAreaProps, never>;
export {};
