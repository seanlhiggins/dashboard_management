import { CompatibleHTMLProps, LayoutProps } from '@looker/design-tokens';
import { FC } from 'react';
interface DialogStyleProps extends LayoutProps, CompatibleHTMLProps<HTMLDivElement> {
}
export interface DialogContentProps extends DialogStyleProps {
    hasFooter?: boolean;
    hasHeader?: boolean;
}
export declare const DialogContent: FC<DialogContentProps>;
export {};
