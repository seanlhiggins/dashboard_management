import { Transitions } from '@looker/design-tokens';
interface UseAnimationStateReturn {
    className: string;
    renderDOM: boolean;
    busy: boolean;
}
export interface AnimationStateConfig {
    exit: boolean;
    enter: boolean;
}
export declare const useAnimationState: (isOpen: boolean, enter?: Transitions, exit?: Transitions) => UseAnimationStateReturn;
export {};
