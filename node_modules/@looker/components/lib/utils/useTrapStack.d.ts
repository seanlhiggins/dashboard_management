import { TrapStackContextProps } from '@looker/components-providers';
import { Context, Ref } from 'react';
export interface UseTrapStackBaseProps<Element extends HTMLElement = HTMLElement> {
    disabled?: boolean;
    ref?: Ref<Element>;
}
export interface UseTrapStackProps<Element extends HTMLElement = HTMLElement, Options extends {} = {}> extends UseTrapStackBaseProps<Element> {
    context: Context<TrapStackContextProps<Options>>;
    options?: Options;
}
export declare const useTrapStack: <E extends HTMLElement = HTMLElement, O extends {} = {}>({ context, disabled, ref, options, }: UseTrapStackProps<E, O>) => [E | null, (node: E | null) => void];
