import React, { ReactNode } from 'react';
import { TextColorProps, TypographyProps, PaddingProps } from '@looker/design-tokens';
interface MenuHeadingProps extends TextColorProps, TypographyProps, PaddingProps {
    children: ReactNode;
    className?: string;
}
export declare const MenuHeading: import("styled-components").StyledComponent<React.FC<MenuHeadingProps>, import("styled-components").DefaultTheme, {}, never>;
export {};
