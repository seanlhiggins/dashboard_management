import { FC } from 'react';
import { ProgressCircularSizes } from './size';
export interface ProgressCircularProps {
    size?: ProgressCircularSizes;
    progress?: number;
    label?: string;
}
export declare const ProgressCircular: FC<ProgressCircularProps>;
