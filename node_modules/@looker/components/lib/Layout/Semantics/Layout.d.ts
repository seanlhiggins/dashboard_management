import { CompatibleHTMLProps } from '@looker/design-tokens';
import { SimpleLayoutProps } from '../utils/simple';
export interface LayoutProps extends SimpleLayoutProps, CompatibleHTMLProps<HTMLElement> {
    fixed?: boolean;
    hasAside?: boolean;
}
export declare const Layout: import("styled-components").StyledComponent<"div", import("styled-components").DefaultTheme, LayoutProps, never>;
