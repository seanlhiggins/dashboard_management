"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.listItemBackgroundColor = void 0;

var _designTokens = require("@looker/design-tokens");

var _styledComponents = require("styled-components");

const listItemBackgroundColor = ({
  keyColor,
  current,
  disabled,
  hovered,
  selected,
  theme: {
    colors
  }
}) => {
  const keyColors = {
    all: colors.keySubtle,
    hovered: colors.ui1,
    selected: colors.keySubtle
  };
  const uiColors = {
    all: (0, _designTokens.itemSelectedColor)(colors.ui2),
    hovered: colors.ui1,
    selected: (0, _designTokens.itemSelectedColor)(colors.ui2)
  };
  const stateColors = keyColor ? keyColors : uiColors;
  let renderedColor;
  if (disabled) renderedColor = 'transparent';else if ((selected || current) && hovered) renderedColor = stateColors.all;else if (selected || current) renderedColor = stateColors.selected;else if (hovered) renderedColor = stateColors.hovered;else renderedColor = 'transparent';
  return (0, _styledComponents.css)(["background:", ";"], renderedColor);
};

exports.listItemBackgroundColor = listItemBackgroundColor;
//# sourceMappingURL=listItemBackgroundColor.js.map