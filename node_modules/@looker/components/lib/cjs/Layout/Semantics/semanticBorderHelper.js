"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.borderHelper = void 0;

require("core-js/modules/es.array.map.js");

var _styledComponents = require("styled-components");

const borderPropertyHelper = (color, property) => (0, _styledComponents.css)(["", ":1px solid ", ";"], property, ({
  theme
}) => theme.colors[color]);

const borderPositionHelper = (border, position) => {
  if (!border) return null;
  const color = border === true ? 'ui2' : border;
  let properties = [];

  switch (position) {
    case 'x':
      properties = ['border-left', 'border-right'];
      break;

    case 'y':
      properties = ['border-bottom', 'border-top'];
      break;

    case undefined:
      return (0, _styledComponents.css)(["", ""], borderPropertyHelper(color, 'border'));

    default:
      properties = [`border-${position}`];
  }

  return (0, _styledComponents.css)(["", ""], properties.map(property => borderPropertyHelper(color, property)));
};

const borderHelper = ({
  border,
  borderBottom,
  borderLeft,
  borderRight,
  borderTop,
  borderX,
  borderY
}) => (0, _styledComponents.css)(["", " ", " ", " ", " ", " ", " ", ""], border && borderPositionHelper(border), borderBottom && borderPositionHelper(borderBottom, 'bottom'), borderLeft && borderPositionHelper(borderLeft, 'left'), borderRight && borderPositionHelper(borderRight, 'right'), borderTop && borderPositionHelper(borderTop, 'top'), borderX && borderPositionHelper(borderX, 'x'), borderY && borderPositionHelper(borderY, 'y'));

exports.borderHelper = borderHelper;
//# sourceMappingURL=semanticBorderHelper.js.map