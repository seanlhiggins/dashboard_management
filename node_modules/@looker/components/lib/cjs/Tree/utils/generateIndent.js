"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.generateIndent = void 0;

var _styledComponents = require("styled-components");

var _indicatorDefaults = require("./indicatorDefaults");

const generateIndent = (depth, indicatorSize, theme) => {
  const sizes = theme.sizes,
        space = theme.space;
  const indicatorGap = _indicatorDefaults.indicatorDefaults.indicatorGap;
  const treePaddingSize = space.xxsmall;
  const indicatorIconSize = sizes[indicatorSize];
  const indicatorGapSize = space[indicatorGap];
  const indentCalculation = `${treePaddingSize} + (${indicatorIconSize} + ${indicatorGapSize}) * ${depth}`;
  return (0, _styledComponents.css)(["padding-left:calc(", ");"], indentCalculation);
};

exports.generateIndent = generateIndent;
//# sourceMappingURL=generateIndent.js.map