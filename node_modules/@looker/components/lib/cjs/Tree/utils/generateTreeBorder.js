"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.generateTreeBorder = void 0;

var _styledComponents = require("styled-components");

var _indicatorDefaults = require("./indicatorDefaults");

const generateTreeBorder = (depth, indicatorSize, theme) => {
  const sizes = theme.sizes,
        space = theme.space;
  const indicatorGap = _indicatorDefaults.indicatorDefaults.indicatorGap;
  const itemBorderSize = '1px';
  const itemPaddingSize = space.xxsmall;
  const indicatorIconSize = sizes[indicatorSize];
  const indicatorGapSize = space[indicatorGap];
  const depthSize = `${itemPaddingSize} + (${indicatorIconSize} + ${indicatorGapSize}) * ${depth}`;
  const borderSpacer = `(${indicatorIconSize} + ${itemBorderSize}) / 2 + ${depthSize}`;
  const preBorderStop = `calc(${borderSpacer} - ${itemBorderSize})`;
  const postBorderStop = `calc(${borderSpacer})`;
  return (0, _styledComponents.css)(["background:linear-gradient( 90deg,transparent ", ",", " ", " ", ",transparent ", " );"], preBorderStop, theme.colors.ui2, preBorderStop, postBorderStop, postBorderStop);
};

exports.generateTreeBorder = generateTreeBorder;
//# sourceMappingURL=generateTreeBorder.js.map